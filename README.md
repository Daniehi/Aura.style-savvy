# AURA.STYLE-SAVVY
# Style-Savvy — Aura Fashion AtelierA luxury fashion landing page for **Aura**, a high-end fashion atelier. Built with React + Vite (frontend) and Express (backend API).## What's included- Full single-page landing site with scroll animations (Framer Motion)- Hero, About, Collections archive, Services, Process, Testimonials, and Contact sections- Contact form with email and WhatsApp links- Responsive layout, serif/sans-serif typography- shadcn/ui component library- Express API server (TypeScript)- Drizzle ORM + PostgreSQL setup (ready to connect)- pnpm monorepo workspace## Project structure```├── artifacts/│   ├── fashion-design/     # React + Vite frontend│   │   └── src/│   │       ├── pages/      # home.tsx, not-found.tsx│   │       ├── components/ # layout (navbar, footer) + shadcn/ui│   │       ├── assets/     # hero.png + 6 collection images│   │       └── index.css   # global styles & theme tokens│   └── api-server/         # Express 5 API server│       └── src/│           ├── app.ts│           ├── index.ts│           └── routes/     # health check + index├── lib/│   ├── api-spec/           # OpenAPI spec (openapi.yaml)│   ├── api-client-react/   # Generated React Query hooks│   ├── api-zod/            # Generated Zod schemas│   └── db/                 # Drizzle ORM schema & config├── package.json├── pnpm-workspace.yaml└── tsconfig.base.json```## Getting started### Requirements- Node.js 20+- pnpm (`npm install -g pnpm`)- PostgreSQL (optional — only needed for DB features)### Install dependencies```bashpnpm install```### Run the frontend```bashpnpm --filter @workspace/fashion-design run dev```### Run the API server```bashpnpm --filter @workspace/api-server run dev```### Environment variables| Variable | Description ||---|---|| `DATABASE_URL` | PostgreSQL connection string (e.g. `postgresql://user:pass@localhost:5432/dbname`) || `PORT` | Port for the API server (defaults to 5000) |### Typecheck everything```bashpnpm run typecheck```### Regenerate API client from OpenAPI spec```bashpnpm --filter @workspace/api-spec run codegen```## Stack- **Frontend**: React 18, Vite, TypeScript, Tailwind CSS, shadcn/ui, Framer Motion- **Backend**: Express 5, TypeScript, Pino (logging)- **Database**: PostgreSQL, Drizzle ORM- **Validation**: Zod- **Monorepo**: pnpm workspaces## 
Contact## danielehimare123@gmail.com  WhatsApp: +234 916 855 3412
